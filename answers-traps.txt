1. Which registers contain arguments to functions? For example, which register holds 13 in main's call to printf?
ANSWER: a0-a8

2. Where is the call to function f in the assembly code for main? Where is the call to g? (Hint: the compiler may inline functions.)
ANSWER: no call to f & g, `pc=2e` directly load 12, which is the result of f(8)+1, to a1.

3. At what address is the function printf located?
ANSWER: 65a

4. What value is in the register ra just after the jalr to printf in main?
ANSWER: 0x40. 'jalr	1570(ra) # 65a <printf>' will do two things: setting ra = next PC addr & set PC as ra+0x1570; 

5. What is the output of the code? in big-endian, do we need change i to get same result, do we need change 57616?
ANSWER: HE110 World.  change i = 0x00726c64; dont need to change 57616, because %x is the hex of the num.

6. 	printf("x=%d y=%d", 3); what is going to be printed after 'y='? 
ANSWER: that's an undefined behavior. printf think we will pass three parameters, so it will read a0,a1,a2 register.
      and use the value in a2 as y=%d. but a2 is not set correctly, so we read a wrong num.
